/**
 * 마커 팔레트 — C2 에서 `palette.ts` 로 흡수됐다.
 *
 * 값과 설명은 그쪽에 있다. 이 파일은 기존 import 경로를 깨지 않기 위한
 * re-export 만 남긴다.
 */
export {
  ALERT_LEVEL_COLORS,
  CROWD_STATUS_COLORS,
  FACILITY_COLORS,
  FACILITY_PIN_STROKE_COLOR,
  SEGMENT_CASING_COLORS,
  SEGMENT_UNKNOWN_COLOR,
} from './palette';
